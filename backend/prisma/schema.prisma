generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         Int    @id @default(autoincrement())
  username   String @unique @db.VarChar(255)
  fortyTwoId Int?   @unique

  password String? @db.VarChar(255)
  twoFa    Boolean @default(false)

  // avatarUrl String @db.VarChar(255)

  // friendIds Int[] @relation(?)
  // blockedIds
  // matchs @relation(?)

  // wins Int
  // losses Int
  // mmr Int
  // stats ...
}

model Channel {
id       Int    @id @default(autoincrement())

// name, messagesList, userlist, adminlist, owner
}

// model Messages {
// id       Int    @id @default(autoincrement())
// channel, sender, content
// }

// model Match {
// id       Int    @id @default(autoincrement())
// match history stats (winner, loser, date, stats, etc)
// }

model Member{
  id String @id @default(uuid())
  role MemberRole @default(GUEST)

  
}

enum MemberRole {
  ADMIN
  MODERATOR
  GUEST
}